export enum RequestType {
    MOVIE = 'movie',
    TV = 'tv',
}

export enum RequestActionType {
    MOVIE = 'Movie',
    TV = 'Tv',
    MUSIC = 'Music'
}

export enum RequestAction {
    APPROVE = 'approve',
    DENY = 'deny'
}